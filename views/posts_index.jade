extends layout

block content
  a(href="/posts/new")
    +button("New Post", "green_bkg")
  each post in posts
    .post
      img(src="#{post.image}", alt="post.title")
      h2= post.title
      p= post.image
      p= body_preview(post.body)